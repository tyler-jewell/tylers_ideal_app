/// https://velocityx.dev/docs/extensions/context#theme

/// context.theme = Theme.of(context)
/// context.textTheme = Theme.of(context).textTheme
/// context.captionStyle = Theme.of(context).textTheme.caption

library;
