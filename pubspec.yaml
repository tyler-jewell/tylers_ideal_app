name: vx_agent_builder
version: 0.0.1
description: A velocity x agent builder package.
publish_to: 'none'

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  velocity_x: ^4.1.2

dev_dependencies:
  melos: ^4.0.0

